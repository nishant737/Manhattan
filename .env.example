# Google Apps Script web-app URL that receives form submissions and appends
# them as rows to the connected Google Sheet.
# Copy this file to `.env`, paste your deployed web-app URL, and restart `npm run dev`.
VITE_LEADS_ENDPOINT=
